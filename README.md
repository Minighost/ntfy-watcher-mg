A short Python script to listen for ntfy broadcasts.

Make sure to change the URL and add your topics as necessary.

I personally build the file into an executable with `pyinstaller`, then run it on user login with Task Scheduler on Windows.

Not sure if this works on Linux.
